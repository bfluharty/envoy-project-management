# envoy-project-management

## Running locally with Docker
1. Create `.env` file by copying `.env.example` and filling in secrets.
2. Run `./run-docker.sh` - as you make changes, the service will rebuild itself automatically.
3. When finished, `CTRL-C` will stop the container.