FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
# Use public registry so install works when private registry is unreachable (e.g. in Docker)
RUN npm ci --registry https://registry.npmjs.org/

EXPOSE 8080

CMD ["npm", "run", "dev"]